# Cognito (same pool/client as apps/api — VITE_ prefix for browser build)
VITE_COGNITO_USER_POOL_ID=ap-southeast-2_n43kZ8c8o
VITE_COGNITO_CLIENT_ID=71bkm0v7u2tabcqvem8nvs7lmp
VITE_COGNITO_REGION=ap-southeast-2

# Sentry (optional local testing; prod from SSM /echotype/SENTRY_DSN_WEB via CI)
# VITE_SENTRY_DSN=
# VITE_SENTRY_RELEASE=local

# Auth Phase 4 probe Part B (optional; keep in local .env only):
# PROBE_COGNITO_AUTH=1
# TEST_USER_EMAIL=...
# TEST_USER_PASSWORD=...
