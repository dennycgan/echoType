DATABASE_URL=postgresql://echotype:echotype@localhost:5432/echotype
API_PORT=3001
WEB_ORIGIN=http://localhost:5173

# Cognito (from terraform output or SSM /echotype/COGNITO_*)
COGNITO_USER_POOL_ID=ap-southeast-2_n43kZ8c8o
COGNITO_CLIENT_ID=71bkm0v7u2tabcqvem8nvs7lmp
COGNITO_REGION=ap-southeast-2

# Sentry (optional local testing; prod from SSM /echotype/SENTRY_DSN_API)
# SENTRY_DSN=
# SENTRY_ENVIRONMENT=development
# SENTRY_RELEASE=local
# SENTRY_DEBUG=1

# Local dev seed only (not used by API runtime after Auth Phase 3)
# SEED_ENV=dev pnpm --filter @echotype/api seed

# Auth Phase 3 probe Part B (optional; keep TEST_USER_* in local .env only):
# TEST_ACCESS_TOKEN='eyJ...'
# PROBE_COGNITO_AUTH=1
# TEST_USER_EMAIL=...
# TEST_USER_PASSWORD=...
